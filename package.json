{
  "name": "cronache-del-destino",
  "version": "6.1.0",
  "private": true,
  "description": "Motore GDR narrativo browser-only con memoria multilivello e provider Ollama.",
  "scripts": {
    "test": "node tests/run-tests.js",
    "check": "node tests/check-html-script.js && node --check js/memory-manager.js && node --check js/narrative-master.js && node --check js/ollama-cloud.js"
  }
}
